# Implementation Rules

> Status: FINAL FROZEN BASELINE v1.0 — subordinate to FINAL_SPECIFICATION/.

## Purpose
Control coding behavior.

## Rules

Implement approved contracts only; stop on ambiguity; contract-first; deterministic critical paths; idempotent mutations; no secrets; no governance bypass; sync docs/tests with behavior changes.
